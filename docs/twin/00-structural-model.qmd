---
title: "1 · Structural model"
---

The **structural model** is the fine, time-invariant description of *what the subsurface is* — its
terrain, soils, geology, and mechanical stiffness. These are the **static layers**: the prior that sets
what the ground *can* hold, where water sits, and how stiff it can be. The dynamic states (water table,
soil moisture, velocity change) are built by modulating this fixed skeleton in time; this chapter is
about how the skeleton itself is aggregated and delivered.

Static layers are the only inputs that carry genuine **90 m spatial texture** — the coarse dynamic
drivers (precipitation, satellites) cannot. So the whole product's spatial resolution is inherited from
these layers, which is why building them correctly, and at the right resolution, comes first.

## Building the static layers

Each layer is fetched from its authoritative source, cleaned, reprojected to the analysis CRS
(EPSG:5070), and resampled to the common 90 m grid. The build steps:

- **Terrain** — the USGS **3DEP** DEM (10 m) is the parent. From it we derive **HAND** (Height Above
  Nearest Drainage — 0 in valley floors, large on ridges), **TWI** (topographic wetness index,
  $\ln(a/\tan\beta)$), and **slope**, using flow routing on the DEM before downsampling to 90 m.
- **Soil** — **SOLUS100** (100 m, USDA-NRCS ML digital soil mapping) supplies the hydromechanical
  texture set: clay / sand / silt fractions, pH, CEC, oven-dry bulk density, and depth-to-lithic (soil
  thickness). Windowed cloud-optimised reads, averaged over the near-surface horizon.
- **Near-surface stiffness** — **Vs30** from the measurement-based **Soil Velocity Model** (SVM;
  Grant, Wirth & Stone 2025), the shallow-soil layer of the USGS Cascadia velocity model. Its Vs(z)
  grid is sampled every 10 m over the top 100 m, and Vs30 is the *travel-time* average
  $30/\int_0^{30}\!dz/V_S$ of the 30 m **below each column's own ground surface** — the model carries
  topography, so a fixed depth window would sample air on a ridge and deep rock in a valley. The
  Wald–Allen topographic-slope proxy remains the always-available fallback where the SVM is not staged.
- **Geology / lithology** — surficial-geology polygons rasterised to 90 m through a lithology crosswalk,
  giving a categorical material class per cell.
- **Subsurface geometry** — **depth-to-bedrock** and **distance-to-coast**, resampled to 90 m; combined
  with HAND and lithology into a **hydrogeologic-domain** mask that stratifies the later models.

Every step is provenance-tracked (source → operation → native resolution → target resolution), so a
downstream consumer can see exactly how each 90 m layer was made.

## Target resolution: 90 m

The target is **90 m, EPSG:5070** — set by the hazard, not the data. Liquefaction in valley fill,
shallow landslides on hillslopes, and compound flooding all resolve at ~100 m or finer, so a coarser
grid would blur the valley-vs-ridge distinction that governs the hazard. Two consequences follow:

- **Fine sources are downsampled, coarse sources are upsampled — honestly.** 3DEP (10 m) is aggregated
  to 90 m (a genuine down-sample); SOLUS (100 m) is resampled to 90 m (near-identity); the SVM Vs grid
  (200 m) is *up*-sampled to 90 m and so gains **no** new information. The provenance records each
  case, so a resampled cell is never mistaken for a native observation.
- **90 m is the resolution the dynamic states inherit.** Because the coarse precipitation/satellite
  drivers carry the *time* signal but not the *space* texture, the 90 m static envelope is what gives
  the final water-table, soil-moisture, and stiffness fields their spatial detail.

## The static-layer catalog

The static layers required by the soil-hydromechanical state and the downstream hazards, each built to
the same 90 m analysis grid (@fig-static-layers).

The analysis domain is the **western Cascades**: EPSG:5070, 90 m, **2.96 M cells**, spanning the Puget
lowland through the Puyallup and Nisqually headwaters. The extent is set by what the model must be able
to *constrain*, not by what is convenient to fetch — the gauged basins are the only observational handle
on the water-budget flux partition (see [Forecast](06-forecast.qmd)), and all eight of them lie
inside it.

![**The static-layer catalog** over the western Cascades domain: 90 m, EPSG:5070, 2.96 M cells. Terrain
derivatives (HAND / slope / TWI) from the 3DEP DEM, routed at 10 m before downsampling; SOLUS100 soil
texture and organic matter; the Saxton–Rawls hydraulic envelope ($K_{sat}$); the baseline water table;
and **Vs30 from the Soil Velocity Model** [@grant2025svm]. **Root depth** is derived from soil
thickness rather than assumed constant, and thins on the ridges (1.05 m on steep ground against 1.49 m
on the flats) — a thin, rocky mountain soil cannot buffer a storm the way a metre of lowland till can,
and root depth scales that storage directly. Regenerate with `pixi run domain-figures`.
](assets/static_layers_domain.png){#fig-static-layers}

::: {.content-visible when-format="html"}
![**The same catalog, one layer at a time** (3 s/frame, looping): each panel shown at full size
instead of shrunk to a ninth of the grid above, with the label inside the frame rather than as a
title above it. Regenerate with `pixi run domain-layers-gif`.
](assets/static_layers_domain.gif){#fig-static-layers-gif}
:::

::: {.content-visible when-format="pdf"}
**The same catalog, one layer at a time** is animated (GIF): each of the nine panels above shown at
full size for 3 seconds, looping — see the animation at the
[online chapter](https://gaia-hazlab.github.io/gwl-space-time-smooth/twin/00-structural-model.html#fig-static-layers-gif).
:::

::: {.callout-important}
## Vs30 is 200 m information on a 90 m grid
The SVM shallow-Vs grid is **200 m** horizontally, so placing it on the 90 m analysis grid is an
*upsample*: it adds no new spatial information, and the apparent 90 m texture in the Vs30 panel is
really 200 m texture. The provenance records this, and the honest reading is that Vs30 carries the
**coarsest native resolution of any static layer** in the catalog — a real limit on how finely
liquefaction stiffness can be resolved, and a reason the site condition is delivered as a *site-class
probability* rather than a point value (see [Hazard integration](07-hazard-integration.qmd)).

Two further properties of the extraction bear stating. The depth axis is **terrain-following**: the
shallowest valid sample has a median $V_S$ of ~207 m s⁻¹ — soil, not rock — so $z=0$ is the ground
surface and the 30 m average is genuinely near-surface, including over high relief. And cells with no
ground, a data gap inside the top 30 m, or less than 30 m of grid below the surface are returned as
**NaN**, never as a filled value.
:::

Over the domain the resulting field is **NEHRP C for 88% of cells, D for 11%, and E for 1%** (median
Vs30 480 m s⁻¹). Vs30 tracks the terrain as it must — valley floor 378, lower slope 433, ridge
528 m s⁻¹ — and the soft D/E tail is the valley fill that carries the liquefaction hazard.

Additional static layers used by the pipeline but not shown above (lithology / geology, depth-to-bedrock,
distance-to-coast, and the derived hydrogeologic-domain mask) are built the same way and enter the
domain stratification.

| Layer | Measures | Source (native → 90 m) | Role in hydromechanics / hazards |
|---|---|---|---|
| HAND | height above nearest drainage | 3DEP DEM 10 m → 90 m | shallow water-table predictor; valley-fill = liquefaction, hillslope = landslide |
| TWI | topographic wetness $\ln(a/\tan\beta)$ | 3DEP 10 m → 90 m | TOPMODEL lateral-flow redistribution of the water table |
| Slope | surface gradient | 3DEP 10 m → 90 m | infinite-slope Factor of Safety; Vs30 proxy input |
| Clay / sand / silt | soil texture fractions | SOLUS100 100 m → 90 m | Saxton–Rawls hydraulic envelope; friction / cohesion |
| pH · CEC · bulk density | soil chemistry / density | SOLUS100 100 m → 90 m | $K_{sat}$ pedotransfer; density in $V_S=\sqrt{\mu/\rho}$ |
| Soil thickness | depth to lithic contact | SOLUS100 100 m → 90 m | storage capacity; transmissivity $T=K_{sat}h$ |
| Lithology / geology | surficial material class | geology polygons → 90 m raster | hydrogeologic domain; stiffness control |
| Vs30 | 30 m time-averaged $V_S$ | SVM shallow-Vs grid, 200 m → 90 m (Wald–Allen fallback) | NEHRP site class; liquefaction stiffness |
| Root depth | soil thickness | SOLUS100 depth-to-restrictive-layer → 90 m | root-zone storage; the buffer a storm fills |
| Depth to bedrock | subsurface geometry | subsurface product → 90 m | aquifer bottom; domain stratification |
| Distance to coast | coastal proximity | derived → 90 m | sea-level / tidal boundary conditioning |

Together these form the static envelope; the [Input data](01-input-data.qmd) chapter covers the
dynamic ground-sensor and satellite streams that modulate it, and the [Physics](03a-physics-hydrology.qmd)
chapters show how the envelope enters the water budget and the petrophysical rheology.

## The baseline water table: observation-anchored, not assumed

One static layer is built differently from the rest: `baseline_dtw_m.tif` (and its elevation
counterpart `baseline_wte_m.tif`) is **regressed from real GWL observations**, not just
resampled from a source raster like HAND or SOLUS. It is Stage 1 of the GWL pipeline
(`src/models/baseline_regression.py`) and answers a specific question: what is the *long-term*
water table, before any dynamic forcing is added on top?

**Target.** Each NWIS well contributes its **long-term median** depth-to-water
(`median_dtw_m`), computed over that well's own period of record — the archive pull starts
2000-01-01 and runs open-ended to whenever it was last refreshed (`pixi run data`), so this is a
multi-year climatological median per well, not a value tied to one calendar date. Wells deeper
than a configurable screen (`--max-well-depth-m`, ~30 m for the Puget Lowland) are dropped: HAND
predicts the shallow, terrain-following table, and pooling in deep-confined wells whose head is
not terrain-driven would corrupt the fit (issue #46).

**Predictors.** The random forest is trained on the static layers above — HAND, TWI, slope, SOLUS
soil texture/$K_{sat}$, PRISM climate, Vs30, depth-to-bedrock — and deliberately **excludes
coordinates**, so it cannot memorise where the training wells sit and must generalise from
physical covariates alone. It is *not* initialized from, or blended with, an external WTD product
(Fan et al. 2013, HydroGEN, or the newer Ma et al. [-@ma2026highres] high-resolution product) — those
appear only as literature context; the baseline here is fit from scratch on this project's own
static layers and its own wells.

**Residual correction.** The random forest alone leaves spatially structured residuals at the
wells; those are kriged (ordinary kriging on a normal-score transform) and added back, so
$\text{DTW}_{\text{final}} = \text{DTW}_{\text{RF}} + \text{residual}_{\text{kriged}}$ — a
regression-kriging hybrid, not a pure ML fit.

**Uncertainty.** Two components are propagated, not just the point estimate: $\sigma_{\text{rf}}$
(the random-forest tree-ensemble spread — how much the trees disagree at each cell, a covariate-
uncertainty term) and $\sigma_{\text{krige}}$ (the ordinary-kriging variance on the residual field
— a spatial-interpolation term, larger far from wells). `src/evaluation/uncertainty_stack.py`
combines these in quadrature, together with the Stage-2 climate-response residual term, into one
tracked $\sigma_{\text{total}}$ — the same static/dynamic/downscaling decomposition the
[assimilation chapter](04-assimilation.qmd#the-uncertainty-budget-static-dynamic-downscaling-observability)
uses everywhere else. It is not just computed but checked: per-domain interval coverage, PIT, and
CRPS (`coverage_metrics.json`) confirm the random-forest spread is calibrated — that 90% intervals
actually contain the held-out truth ~90% of the time — rather than trusting an uncalibrated spread
at face value.

Regenerate with `pixi run baseline` (pilot domain) or `pixi run baseline-wt-domain` (the extended
western-Cascades domain).
